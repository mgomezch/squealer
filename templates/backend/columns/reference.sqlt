create table #{tablename}.#{colname <> " reference"}
  ( "entry" bigint not null primary key references #{tablename}."journal"
  , #{colname <> " reference"} bigint not null references #{target}."journal" ("entry") deferrable initially deferred
  )
;