create table #{tablename}.#{colname <> " proxy"}
  ( "entry" uuid not null primary key references #{tablename}."journal"
  , #{colname <> " state"} uuid not null references #{tablename}.#{colname <> " state"}
  )
;
